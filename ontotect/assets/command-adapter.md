---
description: {{short_description}}
---

Load the installed `{{skill}}` skill and follow its `SKILL.md` as the behavioral
source of truth.

{{instruction}}

Treat the following as the user's target, context, constraints, and requested
outcome:

$ARGUMENTS

This adapter only exposes host command discovery. It does not replace the
Ontotect command contract, grant mutation authority, or establish ontology
evidence.
